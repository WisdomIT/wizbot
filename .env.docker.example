# docker compose --env-file .env.docker 용 (로컬 통합 실행)
MYSQL_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me-root
JWT_SECRET=change-me-jwt            # web/api 공유
INTERNAL_API_TOKEN=change-me-token  # api/chatbot 공유
PUBLIC_SITE_URL=http://localhost:3001
CHZZK_ID=
CHZZK_SECRET=
CHZZK_BOT_CHANNEL_ID=
# SMTP 폴백 (#215) — 어드민 「알림 설정」의 DB 설정이 없을 때 사용
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_SENDER=
